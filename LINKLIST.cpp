#include<bits/stdc++.h>
using namespace std;
struct node{
int info;
struct node *next;
}*head;
//typedef struct node ;
void traversing()
{
    struct node *currrent;
    if(head=NULL)
    {
        cout<<"List is Empty"<<endl;
        return;
    }
    currrent=head;
    while(currrent !=NULL)
    {
        cout<<"DATA->"<<currrent->info<<endl;
        currrent=currrent->next;
    }
}
 int main()
{
    int n,data;
    cin>>n;
    head=(struct node*)malloc(sizeof(struct node));
    head->info=95;
    head->next=NULL;
    struct node *newNode,*temp;
    temp=head;
    for(int i=2;i<n;i++)
    {
        newNode=(struct node*)malloc(sizeof(struct node));
        if(newNode == NULL)
        {
            printf("Unable to allocate memory.");
            break;
        }
        cout<<"Enter Info    "<<endl;
        cin>>data;
        newNode->info=data;
        newNode->next=NULL;
        temp->next=newNode;
        temp=temp->next;
    }
    traversing();

}


























































































































































































































































































































































































































































































































































































































































































































